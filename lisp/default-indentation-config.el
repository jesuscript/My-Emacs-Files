;; default indentation. include this in init.el or use domain-specific indentation with indentation-config.el
;; which is git-ignored

(setq js-indent-level 2)
(setq ruby-indent-level 2)
(setq sgml-basic-offset 2)
(setq c-basic-offset 2)

(provide 'default-indentation-config)


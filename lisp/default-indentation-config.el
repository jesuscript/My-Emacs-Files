;;; default-indentation-config.el --- Baseline indentation values -*- lexical-binding: t; -*-
;;; Copy this to indentation-config.el (git-ignored) and tweak per project.

(setq js-indent-level 2
      ruby-indent-level 2
      sgml-basic-offset 2
      c-basic-offset 2)

(provide 'default-indentation-config)
;;; default-indentation-config.el ends here
